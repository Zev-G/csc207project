package interface_adapter;

public class ErrorHandlingModel extends ViewModel<String> {
    /**
     * Constructs a ViewModel with a given view name.
     *
     * @param viewName the name of the view managed by this ViewModel
     */
    public ErrorHandlingModel(String viewName) {
        super(viewName);
    }
}
